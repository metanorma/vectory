# Auto-generated by Cimas: Do not edit it manually!
# See https://github.com/metanorma/cimas
name: rake

on:
  push:
    branches: [ master, main ]
    tags: [ v* ]
  pull_request:

jobs:
  rake:
    # TODO: Switch to main after merging https://github.com/metanorma/ci/pull/124
    uses: metanorma/ci/.github/workflows/inkscape-rake.yml@ghostscript
    secrets:
      pat_token: ${{ secrets.METANORMA_CI_PAT_TOKEN }}
